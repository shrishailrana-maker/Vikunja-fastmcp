interface:
  display_name: "Vikunja FastMCP"
  short_description: "Safe, scoped Vikunja tracker operations"
  default_prompt: "Use $vikunja-fastmcp to manage this work safely in Vikunja."

dependencies:
  tools:
    - type: "mcp"
      value: "vikunja"
      description: "Configured Vikunja FastMCP server"

policy:
  allow_implicit_invocation: true

